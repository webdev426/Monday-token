{
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network rinkeby"
  },
  "dependencies": {
    "hardhat": "^2.9.1"
  },
  "devDependencies": {
    "ethers": "^5.6.0"
  }
}
